meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{host}}/api/auth/logout
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
